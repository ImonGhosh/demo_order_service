[project]
name = "demo-order-service"
version = "0.1.0"
description = "Phase 1 FastAPI demo order service for AI-Ops incident automation testing."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "sqlalchemy>=2.0.30",
    "uvicorn[standard]>=0.30.0",
]

[project.optional-dependencies]
dev = []

[tool.uvicorn]
factory = false
